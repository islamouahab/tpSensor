This project is a real-time temperature and humidity monitoring system built using React, Firebase Firestore, and Firebase Realtime Database.
It allows both users and admins to authenticate, view live sensor data, and manage monitoring centers.
socket is implemented under the firebase realtime database using ref function and onValue()
requirements to use are nodejs and npm
commands: 
npm install
npm start
